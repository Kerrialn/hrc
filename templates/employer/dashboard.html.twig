{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-8">
     EMPLOYER!
    {% if user.resumes %}
        {{ dump(vacancy) }}
    {% endif %}
    
    </div>

    <div class="col-md-4">    
      
    </div>

</div>
{% endblock %}
